:root {
    --bg-color: #f8fafc;
    --card-bg: #ffffff;
    --text-color: #1e293b;
    --primary-color: #6366f1;
    --border-color: #e2e8f0;
}

.dark-mode {
    --bg-color: #0f172a;
    --card-bg: #1e293b;
    --text-color: #f8fafc;
    --border-color: #334155;
}

body {
    font-family: 'Inter', sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    margin: 0;
    transition: 0.3s;
}

.container { max-width: 800px; margin: 0 auto; padding: 20px; }

.navbar {
    display: flex; justify-content: space-between; align-items: center;
    padding: 1rem 2rem; background: var(--card-bg);
    border-bottom: 1px solid var(--border-color);
    position: sticky; top: 0; z-index: 100;
}

.card {
    background: var(--card-bg); padding: 2rem; border-radius: 16px;
    border: 1px solid var(--border-color); margin-bottom: 2rem;
}

.section-header { display: flex; justify-content: space-between; align-items: center; }

.job-card {
    border: 1px solid var(--border-color); padding: 15px;
    border-radius: 12px; margin-top: 10px;
}

.btn-primary {
    background: var(--primary-color); color: white;
    border: none; padding: 10px 20px; border-radius: 8px; cursor: pointer;
}

.btn-outline {
    background: transparent; border: 1px solid var(--primary-color);
    color: var(--primary-color); padding: 8px 16px; border-radius: 8px; cursor: pointer;
}

.map-placeholder {
    height: 300px; background: #cbd5e1; border-radius: 12px;
    display: flex; align-items: center; justify-content: center; position: relative;
}

.map-pin {
    position: absolute; width: 12px; height: 12px;
    background: #ef4444; border-radius: 50%; border: 2px solid white;
}

/* Modal */
.modal {
    display: none; position: fixed; z-index: 2000;
    left: 0; top: 0; width: 100%; height: 100%;
    background: rgba(0,0,0,0.7); backdrop-filter: blur(4px);
}
.modal-content {
    background: var(--card-bg); margin: 15% auto; padding: 50px;
    max-width: 400px; border-radius: 24px; text-align: center;
}
.giant-text { font-size: 2.5rem; color: var(--primary-color); }

/* AI Assistant */
.ai-widget {
    position: fixed; bottom: 20px; right: 20px; width: 300px;
    background: var(--card-bg); border: 1px solid var(--border-color);
    border-radius: 12px; box-shadow: 0 5px 15px rgba(0,0,0,0.1);
}
.ai-header { background: var(--primary-color); color: white; padding: 10px; border-radius: 12px 12px 0 0; }
.ai-body { height: 150px; padding: 10px; overflow-y: auto; font-size: 0.9rem; }
.ai-input { display: flex; padding: 10px; border-top: 1px solid var(--border-color); }
.ai-input input { flex-grow: 1; padding: 5px; border-radius: 4px; border: 1px solid var(--border-color); }

/* Dropdown */
.dropdown-content {
    display: none; position: absolute; right: 20px;
    background: var(--card-bg); border: 1px solid var(--border-color);
    border-radius: 8px; min-width: 150px;
}
.dropdown-content a { display: block; padding: 10px; text-decoration: none; color: var(--text-color); }
.show { display: block; }
